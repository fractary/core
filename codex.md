# Fractary Core

This project uses the `fractary-core` CLI for development operations including repository management, work/issue tracking, documentation, logging, and file storage.

Skills in `plugins/*/skills/` provide detailed guidance for each operation. Read the relevant `SKILL.md` for conventions and workflows.

Run `fractary-core --help` for full CLI usage. Configuration is at `.fractary/config.yaml`.
