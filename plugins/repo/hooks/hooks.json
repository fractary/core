{
  "description": "Copy gitignored .fractary/env/.env* credential files into newly created worktrees",
  "hooks": {
    "WorktreeCreate": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/worktree-create.sh",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
